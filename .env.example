# Base URL for the external REST API consumed by the app.
# Leave unset to default to the same origin /api.
# Point it at the local demo server for offline work: http://localhost:8787/api
VITE_API_URL=http://localhost:8787/api
